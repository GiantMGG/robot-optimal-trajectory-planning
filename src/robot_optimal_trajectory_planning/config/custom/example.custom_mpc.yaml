# do not edit this file, instead, copy it to custom_mpc.yaml and edit the copy
defaults:
  - mpc # the defaults specified in the corresponding classes
  # overrides in defaults list:
  #- override solver: fatrop
  - _self_

# override specific parameters:

problem:
  prediction_horizon: 5

robot:
  collision_model: "spheres"

solver:
  plugin_options:
    jit: false
